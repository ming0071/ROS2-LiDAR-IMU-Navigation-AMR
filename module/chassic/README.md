### 待改善

- 當使用鍵盤控制時，超過 800 ms 沒收到命令會馬上停止，這樣的急停會使 AMR 抖動，可以嘗試加入平滑化的減速方法。
- 當停止時 encoder 或許可以改發 0 數值（加入停止太久的例外處理?）。
- 雖函式名有 ISR，但實際上寫的仍為函式
    - 應具體轉換為 ISR 的寫法，並且不可太過冗長，將函式切分為上、下部分。
    - 浮點數運算置於下半部。
    - 在實際上 ISR 中的變數應該有 volatile 修飾詞，防止編譯器優化導致讀取錯誤。